# Subdirectories to build
SUBDIRS = data po src

desktopdir = $(datarootdir)/applications

desktop_DATA = invertika.desktop

# Extra files to include
EXTRA_DIST = $(desktop_DATA) \
	     invertika.cbp \
	     invertika.menu

# Autopoint m4 stuff
ACLOCAL_AMFLAGS = -I m4
